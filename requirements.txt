# The pinned toolchain, installed into ./.venv by `just setup`.
#
# Third-party linters are not here: each is a pre-commit hook pinned by
# `rev` in .pre-commit-config.yaml, in its own environment. What lives
# here is the hook runner, what this repository's own checks import, and
# the build backend pyproject.toml names.
pre-commit==4.4.0

# scripts/check_frontmatter.py (D-018).
PyYAML==6.0.3

# Build backend, for the editable install in scripts/setup.sh. Pinned so
# --no-build-isolation has something to build with; not the newest
# release, because setuptools 83+ requires Python 3.10, above the
# engine's floor (D-029).
setuptools==82.0.1
