# The check families for this repository, pinned.
#
# `just check` and the commit hook both run this file, so the local
# runners cannot diverge: the commit hook judges what is about to be
# committed, `just check` what is on disk.
#
# Families arrive with the first artifact of their class, never ahead of
# it, so a green gate never says anything about files that are not there.
# Present at step 000: YAML, JSON, POSIX shell, Markdown, repository
# hygiene and security, and whitespace discipline. The guard's own
# liveness gate joined at step 001. Governance frontmatter joined at
# step 003 with the first skill, and brought with it the first `.py`
# file this repository ships, so Python *well-formedness* joined in the
# same breath — it parses, and it carries no debugger left behind.
# Workflow well-formedness joined at step 005 with the first workflow.
# Python style and typing, and TOML's parse check, joined at step 006
# with the engine and pyproject.toml — and both Python tools are pinned
# to the interpreter floor, so that a construct the floor cannot run
# fails here rather than on an operator's machine (D-029).
---
# .claude/refs/ is supplied material whose authority lives at its source
# (CLAUDE.md rule 3), so nothing here lints it. Only behavior-corpus.md
# is still in the repository to exclude: the house conventions beside it
# and .claude/spec-work/ left at step 005 (D-025, D-026) and are
# gitignored, which scripts/check.sh's enumeration already excludes.
exclude: |
  (?x)^(
    \.claude/refs/.*
  )$

repos:
  # Repository hygiene and security, and whitespace discipline. The
  # stock collection of the runner, pinned, no code written.
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      # Whitespace and newline discipline. These are fixers: the commit
      # hook is where they write, and `just check` reverts and reports
      # instead (scripts/check.sh says why).
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix=lf]

      # Hygiene
      - id: check-merge-conflict
      - id: check-added-large-files
        # --enforce-all, because without it the hook intersects its file
        # list with what is *staged*, so it checks nothing at all when
        # run from `just check` or CI.
        args: [--maxkb=512, --enforce-all]
      - id: check-case-conflict
      - id: check-symlinks
      - id: destroyed-symlinks

      # Security — CLAUDE.md rule 5. A committed secret is a rotated
      # secret, so this is the one family whose breach cannot be undone
      # by noticing it afterwards. This project has no secrets story of
      # its own (§15: nothing at runtime touches a network or holds a
      # credential), so what this guards against is an accidental paste,
      # and a pasted private key is the shape that would actually appear.
      - id: detect-private-key

      # Python well-formedness. Not style — that arrives at step 006
      # with the engine. These two ask the same question the rest of
      # this file asks: does the artifact load, and does it carry
      # anything that should never have been committed.
      - id: check-ast
      - id: debug-statements

      # JSON parse. .claude/settings.json is the permission enforcement
      # mechanism itself, so a malformed edit to it fails exactly as
      # quietly as a skill that never loads. YAML needs no hook here:
      # yamllint --strict below already fails on a parse error.
      - id: check-json

      # TOML parse, arriving with pyproject.toml at step 006. The
      # packaging metadata is the repository-installable door; a
      # malformed one fails at somebody else's install, not here.
      - id: check-toml

  # YAML lint. The hook runner's own configuration is the first artifact
  # of this class.
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.37.1
    hooks:
      - id: yamllint
        args: [--strict]

  # POSIX shell lint. shellcheck ships as a pinned binary inside this
  # hook's environment, so it is not a system prerequisite.
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck

  # Markdown and prose lint over the governance documents. In this
  # repository documents are load-bearing.
  - repo: https://github.com/jackdewinter/pymarkdown
    rev: v0.9.39
    hooks:
      - id: pymarkdown
        args: [--config, .pymarkdown.json, scan]

  # Workflow well-formedness, arriving with the first workflow (D-027).
  # yamllint above proves the file is YAML; it cannot tell a workflow
  # from any other mapping, and a file that is valid YAML but not a
  # valid workflow does not fail loudly — GitHub simply declines to run
  # it, which is the same silent non-loading the two local hooks below
  # exist to catch. Schema validation against the vendored SchemaStore
  # copy: pure Python, no network at run time, and no Go or Docker
  # prerequisite, which is what ruled out actionlint here.
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.38.0
    hooks:
      - id: check-github-workflows
      - id: check-dependabot

  # Python style, arriving at step 006 with the engine (D-030). ruff is
  # lint and format in one pinned tool: `ruff-format` asserts the
  # formatting rather than writing it, so this family behaves like every
  # other check here and unlike the fixer hooks above. Its configuration
  # lives in pyproject.toml beside the packaging metadata, `target-version`
  # set to the interpreter floor.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.4
    hooks:
      - id: ruff-check
      - id: ruff-format
        args: [--check]

  # Types, the second half of the floor check (D-030). `python_version`
  # in pyproject.toml is 3.9, so a standard-library name that arrived in
  # 3.10 fails here even though this workstation runs 3.14 — the failure
  # that would otherwise appear as a fail-open hook on an operator's
  # machine. pass_filenames is false because the configuration names what
  # to check: whole packages, so that a change in one module is judged
  # against every caller of it.
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.3.1
    hooks:
      - id: mypy
        pass_filenames: false
        args: [--config-file=pyproject.toml]

  # Two local scripts, two hooks, one subject: the things that break
  # silently. Nothing standard asks what a Claude Code settings file must
  # still be true for, and the guard is this repository's own. D-018 kept
  # them apart — check-guard.sh is stdlib-only and machine-local,
  # check_frontmatter.py imports a pinned PyYAML and runs everywhere.
  #
  # always_run, because what it hunts is invisible to a file list: the
  # guard is gitignored and so never appears in one, and a settings file
  # that quietly stopped pointing at it is unchanged on the commit that
  # matters. Measured on pre-commit 4.4.0: an always_run hook with
  # pass_filenames: false runs even when --files names something else,
  # and its non-zero exit fails the run (.claude/docs/harness.md).
  - repo: local
    hooks:
      # Governance well-formedness for the rituals themselves: every
      # skill and agent definition parses and agrees with its own path.
      # A malformed one does not fail — it silently never loads, and a
      # ritual nobody can invoke is indistinguishable from one nobody
      # needed.
      #
      # The ecosystem asks most of this: `claude plugin validate
      # --strict` catches missing, unparseable and description-less
      # frontmatter. It is not wired in here — it is the operator's
      # unpinned live CLI, and every other tool in this file is pinned
      # by rev (D-021). What it does not catch at all is a `name` that
      # disagrees with its path, which is this script's reason to exist.
      #
      # It runs on the project interpreter rather than an isolated hook
      # environment because it imports PyYAML, pinned in
      # requirements.txt; `.venv` is guaranteed wherever pre-commit
      # runs, since pre-commit itself lives there.
      #
      # always_run with pass_filenames: false. It is a whole-tree check
      # over five small files, so scoping it to changed files would buy
      # nothing and would miss a definition that broke because something
      # else moved. D-022 cut the layout checks, so this hook no longer
      # detects a skill directory with no SKILL.md at all — `claude
      # plugin validate --strict` does, and is the tool to reach for.
      - id: governance-frontmatter
        name: every skill and agent definition loads
        entry: .venv/bin/python scripts/check_frontmatter.py
        language: system
        pass_filenames: false
        always_run: true

      - id: check-guard
        name: the permission boundary is intact
        entry: bash scripts/check-guard.sh
        language: system
        pass_filenames: false
        always_run: true
